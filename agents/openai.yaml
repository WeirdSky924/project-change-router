interface:
  display_name: "Project Change Router"
  short_description: "Route repo changes to the right capability entry"
  icon_small: "./assets/router-icon.svg"
  icon_large: "./assets/router-icon.svg"
  brand_color: "#0F766E"
  default_prompt: "Use $project-change-router to route the change, pick the correct capability entry, and apply the required guardrails before editing code."
policy:
  allow_implicit_invocation: true
